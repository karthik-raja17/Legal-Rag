version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: deploy/docker/Dockerfile
    container_name: legal-rag-app
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    extra_hosts:
      - "host.docker.internal:host-gateway"